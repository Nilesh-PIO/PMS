{
  "name": "pms-e2e",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "End-to-end specs for the Patient Management Application. Playwright is used rather than Cypress because Chrome, Edge and WebKit/Safari are a stated BRD compatibility requirement (C-47).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install-browsers": "playwright install",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^22.15.0",
    "typescript": "^5.9.2"
  }
}
